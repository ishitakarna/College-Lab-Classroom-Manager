class ClassEvent {

  int startMin, duration;
  String title;
  bool free;

  ClassEvent(int startMin, int duration, String title, bool free) {
    this.startMin = startMin;
    this.duration = duration;
    this.title = title;
    this.free = free;

  }
}